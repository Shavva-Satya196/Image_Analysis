export const APP_NAME = "Visionary AI";

// Model to use for multimodal analysis (Images -> Text)
export const GEMINI_MODEL = "gemini-2.5-flash";

export const MAX_FILE_SIZE_MB = 10;
export const ACCEPTED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/heic'];

export const PROMPTS = {
  ANALYZE_IMAGE: "Analyze this image in detail. Describe the visual elements, context, colors, and any text present. Structure your response with a brief summary followed by key details."
};
